<UserControl x:Class="SpellEditor.CopySpellDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Margin="20">

        <TextBlock Text="{DynamicResource CopySpellRecord1}" FontSize="20" Margin="0 0 0 10"/>

        <TextBlock Text="{DynamicResource CopySpellRecord2}"/>
        <TextBox x:Name="OldIdInput" Margin="0 5 0 10"/>

        <TextBlock Text="{DynamicResource CopySpellRecord4}"/>
        <TextBox x:Name="NewIdInput" Margin="0 5 0 20"/>

        <CheckBox x:Name="FirstBox" Content="{DynamicResource CopySpellRecord7}" Margin="0 0 0 10"/>

        <TextBlock Text="{DynamicResource CopySpellRecord8}"/>
        <TextBox x:Name="AmountInput" Margin="0 5 0 10"/>


        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="0 0 10 0"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80"/>
        </StackPanel>

    </StackPanel>
</UserControl>