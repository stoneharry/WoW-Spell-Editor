<controls:MetroWindow x:Class="SpellGUIV2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="Stoneharry's Spell Editor V2"
        Height="450"
        Width="825"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        ShowTitleBar="True"
        ShowIconOnTitleBar="true" Icon="Resources/icon.ico">
    
    <Grid>
        <TabControl HorizontalAlignment="Left"
		    Margin="10,10,0,0"
		    VerticalAlignment="Top"
		    SelectionChanged="TabControl_SelectionChanged">
            <TabItem Header="Load DBC File">
                <Grid>
                    <TextBlock Name="txtLoadedFile"
                   HorizontalAlignment="Left"
			       Margin="10"
			       TextWrapping="Wrap"
			       Text="Loaded file: "
			       VerticalAlignment="Top"/>
                    <Button Content="Load New DBC File" HorizontalAlignment="Left" Margin="23,31,0,-10" VerticalAlignment="Top" Width="175" Click="LoadNewDBCFile"/>
                </Grid>
            </TabItem>
            <TabItem Header="Spell Overview">
                <Grid>
                    <TextBlock HorizontalAlignment="Left"
			       Margin="10"
			       TextWrapping="Wrap"
			       Text="Test."
			       VerticalAlignment="Top"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>

    <controls:MetroWindow.WindowCommands>
        <controls:WindowCommands>
            <Button x:Name="SaveToDBC" Click="SaveToNewDBC">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="4 0 0 0" VerticalAlignment="Center" Text="Save To New DBC File" />
                </StackPanel>
            </Button>
        </controls:WindowCommands>
    </controls:MetroWindow.WindowCommands>

</controls:MetroWindow>